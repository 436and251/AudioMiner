# FFmpeg binaries for Windows packaging

For a self-contained Windows release, place these files here before running `scripts/build_windows.ps1`:

- `ffmpeg.exe`
- `ffprobe.exe`

They are copied to `dist/VoiceDatasetBuilder/bin/`. They are intentionally not committed in this source package.
